# 30-s5-delete-dialog-info-icon

- Number: 30
- Slug: s5-delete-dialog-info-icon

## Notes

- `S5` の削除確認ダイアログで、最後の1件削除時に出す補足ラベルのアイコンを `info.circle` に変更した。

## Verification

- `make build` succeeded.
- `git diff --check` returned no issues.
