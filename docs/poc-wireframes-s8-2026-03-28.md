# CoCo壱番屋 新規アプリ PoC ワイヤー

- 作成日: 2026-03-28
- 対象画面:
  - `S8 Order Complete`
- 関連資料:
  - `docs/poc-app-direction-2026-03-28.md`
  - `docs/poc-screen-flow-2026-03-28.md`
  - `docs/poc-wireframes-s5-s6-2026-03-28.md`

## 目的

この資料は、注文完了を `成功したと分かる` だけでなく、`このあと何をすればよいか分かる` 状態まで設計するためのワイヤーです。

PoCにおける完了画面の役割は次の3つです。

- 注文成功を曖昧にしない
- 受取情報をすぐ理解させる
- お気に入り保存を含む次の行動へ迷わず進ませる

## 前提

- 実注文APIではなくモック成功レスポンス前提
- `S8 Order Complete` は full-screen push
- `Place Order` 押下後は短い処理演出を経て `S8` に遷移する
- 完了画面ではエラー分岐を扱わない
- `この店舗限定` のような発見文脈は S8 まで持ち込まない

## S8 Order Complete

### 画面の役割

- 完了を視覚、文言、触覚で伝える
- 受取情報を明快に見せる
- 完了直後の保存提案と、メニュー再訪、Saved Combos 再訪、店舗変更の導線を整理する

### 情報構造

1. 完了演出
2. 注文完了メッセージ
3. 受取情報
4. 今回の注文サマリー
5. 次の行動CTA

## ワイヤー 1: 完了直後の演出状態

```text
┌─────────────────────────────────────┐
│                                     │
│                                     │
│               [ ✓ ]                 │
│                                     │
│         Order Placed Successfully    │
│                                     │
│      ご注文を受け付けました           │
│                                     │
│          渋谷道玄坂店で受取            │
│          18:20 - 18:35              │
│                                     │
│      ロースカツカレー / 3辛 / 400g     │
│      チーズ / ほうれん草              │
│                                     │
│         1,230円（税込）               │
│                                     │
│                                     │
└─────────────────────────────────────┘
```

### 演出意図

- 最初の1秒前後は `成功した` を最優先で伝える
- 情報量は絞り、感情のピークを作る
- 触覚フィードバックはここで返す

## ワイヤー 2: 落ち着いた情報表示状態

```text
┌─────────────────────────────────────┐
│ Done                                  │
├─────────────────────────────────────┤
│ [ ✓ ] Order Placed                    │
│ ご注文を受け付けました                 │
├─────────────────────────────────────┤
│ Pickup Info                             │
│ ┌─────────────────────────────────┐ │
│ │ Store      渋谷道玄坂店          │ │
│ │ Time       today 18:20 - 18:35  │ │
│ │ Ref        MOCK-20260328-001    │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│ Your Order                              │
│ ┌─────────────────────────────────┐ │
│ │ ロースカツカレー                 │ │
│ │ 3辛 / 400g                      │ │
│ │ チーズ / ほうれん草              │ │
│ │ [ ★ 保存する ]           1,230円│ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│ Next                                   │
│ ・受取時間に店舗へお越しください        │
│ ・このPoCでは注文番号はモック表示です    │
├─────────────────────────────────────┤
│ [ Browse Menu Again ]                   │
│ [ View Saved Combos ]                   │
│ [ Change Store ]                        │
└─────────────────────────────────────┘
```

### ワイヤー 3: 保存完了後の状態

```text
┌─────────────────────────────────────┐
│ [ ✓ ] Order Placed                    │
│ 渋谷道玄坂店 / 18:20 - 18:35          │
├─────────────────────────────────────┤
│ Your Order                              │
│ ┌─────────────────────────────────┐ │
│ │ ロースカツカレー                 │ │
│ │ 3辛 / 400g / チーズ              │ │
│ │ [ 保存済み ]             1,230円│ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│ [ Browse Menu Again ]                   │
│ [ Change Store ]                        │
└─────────────────────────────────────┘
```

用途:

- `S7 Save Favorite Sheet` 完了後に戻る着地点

## S8 コンポーネント意図

- `Success Mark`
  - 一目で完了が分かる中心要素
- `Pickup Info Card`
  - 受取先と受取時間を誤解なく伝える
- `Reference ID`
  - 実運用ではないが、注文が記録された感覚を作る
- `Your Order Card`
  - 最終的に何を注文したかを短く再確認させる
  - 各注文内容の金額表示の左側に、お気に入り保存アクションまたは保存済み状態を置く
- `Save Favorite Action`
  - 完了した注文だけをお気に入り候補として提案する
  - 独立カードにはせず、各注文内容の価格行に `保存する` として置く
  - お気に入りから何も変更せずにカートへ入れた注文内容は `保存済み` と表示する
- `Next Actions`
  - 再利用や再探索の導線を整理する

## S8 重要ルール

- 最初に見せる情報は `成功` と `受取` の2つだけに絞る
- 受取店舗、受取時間、注文内容は必ず一画面で確認できる
- 店舗限定商品であっても、完了画面では `この店舗限定` を表示しない
- 保存提案は出してよいが、完了確認より強く見せない
- 保存提案は独立カードではなく、各注文内容の金額表示の左側に小さなボタンとして見せる
- お気に入りから何も変更せずにカートへ入れた注文内容は、保存ボタンではなく `保存済み` と表示する
- `Browse Menu Again` を主CTAにする
- `View Saved Combos` は副CTAだが十分目立たせる
- `Change Store` は破壊的ではないが、完了後の文脈では最も優先度を下げる

## 推奨CTA優先度

1. `Browse Menu Again`
2. `View Saved Combos`
3. `Change Store`

理由:

- 完了直後は次の注文か、保存済み構成の再利用が自然
- 店舗変更は完了後のメイン欲求ではない

## 状態差分

### S8 の主要状態

1. 成功演出中
2. 情報表示状態
3. 保存完了後状態

### 推奨遷移

```text
S5 Place Order tapped
  -> brief loading / transition
  -> S8 success animation
  -> S8 settled information state
```

## アニメーションと反応の最低要件

- `Place Order` 押下後は 0.4秒前後の短い反応を返す
- S8 到達時は success mark が軽く拡大して止まる
- 触覚フィードバックは成功時に1回
- 成功演出は長くても1.2秒以内に情報表示状態へ移る

## アクセシビリティ注意点

- 完了状態はアイコンだけでなく文言でも示す
- 受取店舗、時間、注文内容、金額は VoiceOver で順に追える構造にする
- CTAは縦積みで押し間違いにくくする
- success animation 中でも Reduce Motion 環境では静的表示へ切り替えられるようにする

## 初版PoCで入れるもの / 後回しにするもの

### 初版PoCで入れる

- 成功演出
- 受取情報表示
- 注文サマリー表示
- 完了直後の保存提案
- `Browse Menu Again`
- `View Saved Combos`

### 後回しにしてよい

- 共有導線
- カレンダー追加
- 通知設定導線

## 次に詰める論点

1. success animation をどの程度リッチにするか
2. `Browse Menu Again` を主CTAにするか、`Done` 的な閉じ動線を主にするか
3. `Reference ID` を PoCで見せるかどうか
4. 複数皿注文時に1皿ずつ保存のままとするか、注文セット保存へ拡張するか
5. `Change Store` を画面上に残すべきかどうか
